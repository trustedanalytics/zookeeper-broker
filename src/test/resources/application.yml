security:
  user.password: fake
broker.root:
  node: /platform
zk:
  cluster:
store:
  path: ${broker.store.znode:/broker/zookeeper/metadata}
cf:
  servicename: zookeeper
  serviceid: zookeeper
  baseId: ${BASE_GUID:}
metadata:
  imageUrl: ${IMAGE_URL:}
